import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  descriptionCard: {
    flexDirection: 'row',
    alignItems: 'baseline',
    gap: 10,
  },
  descriptionText: {
    fontSize: 19,
    fontWeight: '400',
  },
})
