import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  characterWrapper: {
    marginTop: 20,
  },
  listWrapper: {
    width: '100%',
    height: '90%',
    paddingBottom: 150,
  },
})
