import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  paginationWrapper: {
    position: 'absolute',
    paddingHorizontal: 20,
    alignItems: 'center',
    zIndex: 2,
    gap: 5,
    right: 0,
    left: 0,
    bottom: 7,
    flexDirection: 'row',
  },
})
